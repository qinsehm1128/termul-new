/**
 * Thin dialog confirm/open re-exports for components (Story 1.6 Biome ban).
 * Prefer `dialogApi` / `tauriDialogApi` for new call sites.
 */
export { confirm, message, open, save } from '@tauri-apps/plugin-dialog'
